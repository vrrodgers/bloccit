<h1 class="spon" >Sponsored Posts</h1>
<hr>
<h5 class="sponsor-title"><%= @sponsoredpost.title %></h5>
 
  <div class="row">
   <div class="col-md-8">
     <p><%= @sponsoredpost.body %></p>
     <p><%= number_to_currency(@sponsoredpost.price) %></p>
   </div>
   
  </div>
  <%= link_to 'Back to Topic', topic_path(@topic),class: 'btn btn-primary' %> |
  <%= link_to 'Edit', edit_topic_sponsored_post_path(@topic, @sponsoredpost), class: 'btn btn-success' %>