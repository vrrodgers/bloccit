<h1>SponsoredPosts</h1>
<h1><%= @sponsoredpost.title %></h1>
 
  <div class="row">
   <div class="col-md-8">
     <p><%= @sponsoredpost.body %></p>
     <p><%= number_to_currency(@sponsoredpost.price) %></p>
   </div>
   
  </div>
  <%= link_to @topic.name, topic_path(@topic),class: 'btn btn-primary' %>